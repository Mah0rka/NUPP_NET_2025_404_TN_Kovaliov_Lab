# Як відкрити Swagger UI

## ✅ Swagger тепер працює!

Swagger UI виправлено та налаштовано з сумісною версією Swashbuckle.AspNetCore 6.5.0.

---

## 🚀 Запуск Fish.REST з Swagger

### 1. Запустіть сервер:

```powershell
cd C:\Users\user\repos\NUPP_NET_2025_404_TN_Kovaliov_Lab\Fish\Fish.REST
dotnet run
```

### 2. Відкрийте Swagger UI в браузері:

**Головна сторінка Swagger:**
```
http://localhost:5000/swagger
```

або

```
http://localhost:5000/swagger/index.html
```

---

## 📋 Що ви побачите в Swagger UI

Swagger автоматично згенерує інтерактивну документацію для всіх ваших API ендпоінтів:

### **FishesController:**
- `GET /api/fishes` - Отримати всі риби
- `GET /api/fishes/{id}` - Отримати рибу за ID
- `POST /api/fishes` - Створити нову рибу
- `PUT /api/fishes/{id}` - Оновити рибу
- `DELETE /api/fishes/{id}` - Видалити рибу

### **AquariumsController:**
- `GET /api/aquariums` - Отримати всі акваріуми
- `GET /api/aquariums/{id}` - Отримати акваріум за ID
- `POST /api/aquariums` - Створити новий акваріум
- `PUT /api/aquariums/{id}` - Оновити акваріум
- `DELETE /api/aquariums/{id}` - Видалити акваріум

---

## 🧪 Як тестувати API через Swagger

### 1. **GET запит** (отримати всі риби):
   - Натисніть на `GET /api/fishes`
   - Натисніть кнопку "Try it out"
   - Натисніть "Execute"
   - Побачите JSON відповідь з даними

### 2. **GET з пагінацією**:
   - `GET /api/fishes`
   - "Try it out"
   - Введіть параметри: `page = 1`, `amount = 5`
   - "Execute"

### 3. **GET за ID**:
   - `GET /api/fishes/{id}`
   - "Try it out"
   - Введіть Guid з попередньої відповіді
   - "Execute"

### 4. **POST запит** (створити рибу):
   - Натисніть на `POST /api/fishes`
   - "Try it out"
   - Відредагуйте JSON в Request body:
   ```json
   {
     "variety": "Нова риба",
     "habitat": "Прісна вода",
     "topSpeed": 25,
     "isPredatory": false,
     "length": 15.5,
     "aquariumId": null
   }
   ```
   - "Execute"
   - Отримаєте статус 201 Created з даними створеної риби

### 5. **PUT запит** (оновити):
   - `PUT /api/fishes/{id}`
   - "Try it out"
   - Введіть ID риби
   - Відредагуйте JSON
   - "Execute"

### 6. **DELETE запит**:
   - `DELETE /api/fishes/{id}`
   - "Try it out"
   - Введіть ID
   - "Execute"

---

## 📸 Для PDF звіту

### Зробіть скріншоти:

1. **Головна сторінка Swagger** з усіма ендпоінтами
2. **GET /api/fishes** - виконаний запит з відповіддю
3. **GET /api/aquariums** - виконаний запит з відповіддю
4. **POST /api/fishes** - створення нової риби
5. **GET /api/fishes/{id}** - отримання створеної риби
6. **PUT /api/fishes/{id}** - оновлення риби
7. **DELETE /api/fishes/{id}** - видалення риби

### Як зробити скріншот:
- Windows: `Win + Shift + S` або Snipping Tool
- Збережіть у папку `Fish/Screenshots/`

---

## 🔧 Технічні деталі

### Версії пакетів:
- **Swashbuckle.AspNetCore**: 6.5.0 (сумісна з .NET 9.0)
- **Microsoft.OpenApi**: 1.2.3

### Налаштування в Program.cs:
```csharp
// Реєстрація Swagger
builder.Services.AddEndpointsApiExplorer();
builder.Services.AddSwaggerGen();

// Увімкнення Swagger UI в Development режимі
if (app.Environment.IsDevelopment())
{
    app.UseSwagger();
    app.UseSwaggerUI();
}
```

### Swagger JSON:
Доступний за адресою: `http://localhost:5000/swagger/v1/swagger.json`

---

## ⚠️ Важливо

1. **Сервер має бути запущений** на http://localhost:5000
2. **PostgreSQL має працювати** для виконання запитів
3. **Використовуйте Development режим** для доступу до Swagger UI
4. Swagger доступний **тільки в Development**, не в Production

---

## 🎯 Швидкий старт

```powershell
# 1. Запустити сервер
cd C:\Users\user\repos\NUPP_NET_2025_404_TN_Kovaliov_Lab\Fish\Fish.REST
dotnet run

# 2. Відкрити браузер
start http://localhost:5000/swagger

# 3. Тестувати API через Swagger UI!
```

Готово! Тепер можете використовувати Swagger для тестування та документування вашого API! 🎉

